#include<iostream>
#include<vector>

using namespace std;

int main(void){
    int row,col;

    cin>>row>>col;

    vector<vector<int>> arr(row,vector<int>(col));

    for(int i=0;i<row;i++){
        for(int j=0;j<col;j++){
            cin>>arr[i][j];
        }
    }

    vector<int> result;

    for(int i=0;i<)

    return 0;
}